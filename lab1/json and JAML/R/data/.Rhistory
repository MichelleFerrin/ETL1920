knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
library(tidyverse) # trata de datos y visualizacoion
library(ggstatsplot) # visualizacion
datos <- read_json("../data/iris.json")
library(jsonlite) # json en R
library(yaml) #  yaml
datos <- read_json("../data/iris.json")
datos <- read_json("../data/iris.json")
datos
datos <- read_json("../data/iris.json")
datos
datos <- read_json("./data/iris.json")
setwd("~/Documents/CUNEF/ETL/Projects/lab1/json and JAML/R")
datos <- read_json("../data/iris.json")
datos
datos <- read_json("iris.json")
setwd("~/Documents/CUNEF/ETL/Projects/lab1/json and JAML/R/data")
datos <- read_json("../data/iris.json")
datos
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos$142
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos$species
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos$versicolor
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos[1]
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos[1]
## Podemos acceder a los distintos elementos del abjeto que hemos creado de la siguiente manera
datos[1][1]
## Tambien de la siguiente manera
datos$sepalWidth[1]
## Tambien de la siguiente manera
datos$sepalWidth[2]
yaml <- read_yaml("..data/countries.yaml")
yaml <- read_yaml("..data/..countries.yaml")
yaml <- read_yaml("..data../countries.yaml")
yaml <- read_yaml("..data/..countries.yaml")
yaml <- read_yaml("../data/countries.yaml")
yaml <- read_yaml("../data/..countries.yaml")
yaml <- read_yaml("../data/../countries.yaml")
yaml <- read_yaml("../data../countries.yaml")
yaml <- read_yaml("../data/countries.yaml")
yaml
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[1]
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[1][1]
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[1]
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[[1]][1]
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[1][1]
## Podemos acceder a los distintos elementos del objeto que hemos creado de la siguiente manera
yaml[1]
yaml$City
yaml[[1]]
yaml[[1]][1]
